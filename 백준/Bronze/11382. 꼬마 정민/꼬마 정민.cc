#include <stdio.h>
#include <string.h>
#include <algorithm>
#include <iostream>


using namespace std;
char arr[500000][30] = {0};

int main() {
	int num;
	long long int a, b, c;
	scanf("%lld %lld %lld", &a, &b, &c);
	printf("%lld", a + b + c);
	
}