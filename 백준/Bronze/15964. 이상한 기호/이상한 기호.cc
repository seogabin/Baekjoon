#include <stdio.h>
#include <algorithm>
#include <stack>
#include <string.h>
#include <iostream>

#define MAX 100

using namespace std;

int brr[10000]={0};
int main() {
	int a, b;
	scanf("%d %d", &a, &b);
	printf("%d\n", (a + b) * (a - b));

}

	

